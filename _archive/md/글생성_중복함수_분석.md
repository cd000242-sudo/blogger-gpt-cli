# 글 생성 관련 중복/겹치는 함수 분석 및 정리

## 📋 발견된 중복/겹치는 함수 목록

### 1. **src/core/index.ts** - 핵심 생성 함수들

#### ✅ 실제 사용 중인 함수
- `generateMaxModeArticle` (1962줄)
  - **용도**: 실제 글 생성에 사용 (runPost에서 호출)
  - **상태**: ✅ 활성 사용 중
  - **반환값**: `{ html: string; title: string; labels: string[]; thumbnail: string }`

#### ⚠️ 사용 여부 불명확한 함수들
- `generateWordPressContent` (797줄)
  - **용도**: WordPress 전용 콘텐츠 생성
  - **상태**: ⚠️ 정의만 있고 실제 호출 위치 미확인
  - **특징**: JSON 파싱 비활성화, 하드코딩된 HTML만 사용

- `generatePreviewContent` (917줄)
  - **용도**: 미리보기 전용 콘텐츠 생성
  - **상태**: ⚠️ 정의만 있고 실제 호출 위치 미확인
  - **특징**: JSON 파싱 비활성화

- `generateBloggerContent` (999줄)
  - **용도**: Blogger 전용 콘텐츠 생성
  - **상태**: ❌ 하드코딩된 HTML만 반환, 실제 사용 안 함
  - **특징**: 완전히 하드코딩된 HTML 템플릿만 반환

#### 🔄 모드별 생성 함수들
- `generateGptsModeArticle` (1120줄)
  - **용도**: GPTs 모드 (7회차 구조)
  - **상태**: ⚠️ 정의만 있고 실제 호출 위치 미확인

- `generateCustomModeArticle` (1250줄)
  - **용도**: 커스텀 모드 (기본 Gemini 프롬프트)
  - **상태**: ⚠️ 정의만 있고 실제 호출 위치 미확인

#### 🔧 저수준 생성 함수들
- `generateWithOpenAI` (464줄)
  - **용도**: OpenAI 기반 콘텐츠 생성
  - **상태**: ✅ 정의되어 있음
  - **중복**: `src/cli.ts`의 `generatePost`와 기능 중복

- `generateWithGemini` (512줄)
  - **용도**: Gemini 기반 콘텐츠 생성 (7회차 구조)
  - **상태**: ✅ 정의되어 있음
  - **중복**: `src/cli.ts`의 `generatePostGemini`와 기능 중복

### 2. **src/cli.ts** - CLI용 생성 함수들 (중복!)

#### ❌ 중복 함수들 (core/index.ts와 기능 중복)
- `generatePost` (346줄)
  - **용도**: OpenAI 기반 글 생성
  - **중복**: `generateWithOpenAI`와 기능 중복
  - **사용처**: `generatePostByProvider`에서 호출
  - **권장**: `generateWithOpenAI`로 통합 또는 제거

- `generatePostGemini` (401줄)
  - **용도**: Gemini 기반 글 생성
  - **중복**: `generateWithGemini`와 기능 중복
  - **사용처**: `generatePostByProvider`에서 호출
  - **권장**: `generateWithGemini`로 통합 또는 제거

- `generatePostByProvider` (448줄)
  - **용도**: Provider에 따라 선택
  - **사용처**: `main()` 함수에서 호출 (652, 699, 759줄)
  - **권장**: core/index.ts의 함수로 대체

#### 🔧 확장 함수들
- `expandArticleHtml` (417줄)
  - **용도**: OpenAI로 글 확장
  - **사용처**: `expandArticleHtmlByProvider`에서 호출

- `expandArticleHtmlGemini` (436줄)
  - **용도**: Gemini로 글 확장
  - **사용처**: `expandArticleHtmlByProvider`에서 호출

- `expandArticleHtmlByProvider` (453줄)
  - **용도**: Provider에 따라 선택
  - **사용처**: `main()` 함수에서 호출 (659, 705, 765줄)

## 🔍 중복 분석 결과

### 주요 중복 패턴

1. **OpenAI/Gemini 생성 함수 중복**
   - `src/cli.ts`: `generatePost`, `generatePostGemini`
   - `src/core/index.ts`: `generateWithOpenAI`, `generateWithGemini`
   - **문제**: 동일한 기능이 두 곳에 존재

2. **사용되지 않는 함수들**
   - `generateBloggerContent`: 하드코딩된 HTML만 반환, 실제 사용 안 함
   - `generateWordPressContent`: 정의만 있고 호출 위치 미확인
   - `generatePreviewContent`: 정의만 있고 호출 위치 미확인
   - `generateGptsModeArticle`: 정의만 있고 호출 위치 미확인
   - `generateCustomModeArticle`: 정의만 있고 호출 위치 미확인

3. **실제 사용 중인 함수**
   - `generateMaxModeArticle`: `runPost`에서 실제 호출됨
   - `runPost`: 실제 글 생성의 진입점

## 📝 정리 권장사항

### 즉시 제거 가능
1. ❌ `src/cli.ts`의 `generatePost`, `generatePostGemini`, `generatePostByProvider`
   - `src/core/index.ts`의 `generateWithOpenAI`, `generateWithGemini`로 대체
   - `main()` 함수에서 core 함수 직접 호출로 변경

2. ❌ `src/core/index.ts`의 `generateBloggerContent`
   - 하드코딩된 HTML만 반환, 실제 사용 안 함
   - 제거 또는 주석 처리

### 확인 후 정리
3. ⚠️ `generateWordPressContent`, `generatePreviewContent`
   - 실제 사용 여부 확인 후 정리
   - 사용 안 하면 제거, 사용하면 문서화

4. ⚠️ `generateGptsModeArticle`, `generateCustomModeArticle`
   - 실제 사용 여부 확인 후 정리
   - `generateMaxModeArticle`과의 관계 명확화

### 유지
5. ✅ `generateMaxModeArticle` - 실제 사용 중
6. ✅ `generateWithOpenAI`, `generateWithGemini` - 저수준 생성 함수
7. ✅ `runPost` - 실제 진입점

## 🎯 정리 우선순위

1. **높음**: `src/cli.ts`의 중복 함수 제거
2. **중간**: 사용되지 않는 `generateBloggerContent` 제거
3. **낮음**: 사용 여부 불명확한 함수들 확인 후 정리





